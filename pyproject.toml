[project]
name = "deepseek-balance"
version = "0.1.0"
description = "DeepSeek balance desktop widget"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.11.1",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.1",
]
